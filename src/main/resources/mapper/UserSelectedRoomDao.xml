<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.back_students_choose_lodge.dao.UserSelectedRoomDao">

    <resultMap type="com.back_students_choose_lodge.entity.UserSelectedRoom" id="UserSelectedRoomMap">
        <result property="userSelectedRoomId" column="user_selected_room_id" jdbcType="INTEGER"/>
        <result property="uid" column="uid" jdbcType="INTEGER"/>
        <result property="buildingId" column="building_id" jdbcType="INTEGER"/>
        <result property="roomId" column="room_id" jdbcType="INTEGER"/>
        <result property="bedNumber" column="bed_number" jdbcType="INTEGER"/>
    </resultMap>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="UserSelectedRoomMap">
        select
        user_selected_room_id, uid, building_id, room_id, bed_number
        from students_choose_lodge_database.user_selected_room
        <where>
            <if test="userSelectedRoomId != null">
                and user_selected_room_id = #{userSelectedRoomId}
            </if>
            <if test="uid != null">
                and uid = #{uid}
            </if>
            <if test="buildingId != null">
                and building_id = #{buildingId}
            </if>
            <if test="roomId != null">
                and room_id = #{roomId}
            </if>
            <if test="bedNumber != null">
                and bed_number = #{bedNumber}
            </if>
        </where>
    </select>
<!--    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->

    <!--查询单个-->
    <select id="queryById" resultMap="UserSelectedRoomMap">
        select user_selected_room_id,
               uid,
               building_id,
               room_id,
               bed_number
        from students_choose_lodge_database.user_selected_room
        where user_selected_room_id = #{userSelectedRoomId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserSelectedRoomMap">
        select user_selected_room_id,
               uid,
               building_id,
               room_id,
               bed_number
        from students_choose_lodge_database.user_selected_room limit #{offset}, #{limit}
    </select>



    <!--新增所有列-->
    <insert id="insert" keyProperty="userSelectedRoomId" useGeneratedKeys="true">
        insert into students_choose_lodge_database.user_selected_room(uid, building_id, room_id, bed_number)
        values (#{uid}, #{buildingId}, #{roomId}, #{bedNumber})
    </insert>

    <insert id="insertBatch" keyProperty="userSelectedRoomId" useGeneratedKeys="true">
        insert into students_choose_lodge_database.user_selected_room(uid, building_id, room_id, bed_number)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.uid}, #{entity.buildingId}, #{entity.roomId}, #{entity.bedNumber})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="userSelectedRoomId" useGeneratedKeys="true">
        insert into students_choose_lodge_database.user_selected_room(uid, building_id, room_id, bed_number)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.uid}, #{entity.buildingId}, #{entity.roomId}, #{entity.bedNumber})
        </foreach>
        on duplicate key update
        uid = values(uid) , building_id = values(building_id) , room_id = values(room_id) , bed_number =
        values(bed_number)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update students_choose_lodge_database.user_selected_room
        <set>
            <if test="uid != null">
                uid = #{uid},
            </if>
            <if test="buildingId != null">
                building_id = #{buildingId},
            </if>
            <if test="roomId != null">
                room_id = #{roomId},
            </if>
            <if test="bedNumber != null">
                bed_number = #{bedNumber},
            </if>
        </set>
        where user_selected_room_id = #{userSelectedRoomId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from students_choose_lodge_database.user_selected_room
        where user_selected_room_id = #{userSelectedRoomId}
    </delete>

</mapper>

